spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3307/inventory_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true

app:
  inventory:
    low-stock-threshold: 10
