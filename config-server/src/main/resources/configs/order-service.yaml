spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/order_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true

app:
  order:
    max-items-per-order: 10
    pending-timeout-minutes: 30
